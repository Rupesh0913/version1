#include<stdio.h>
int linear(int[],int,int);
int main()
{
	int arr[10],size,i,item,index,pos;
	printf("Enter the elemint\n");
	for(i=0;i<size;i++)
	scanf("%d",&arr[i]);
	printf("Enter the element you want to search:");
	scanf("%d",&item);
	pos=linear(arr,size,item);
	if(index==-1)
	printf("Element is not present");
	else
	printf("\nElement is present at %d position:");
	scanf("%d",&pos+1);
	return 0;
}
int linear(int arr,int size,int item)
{
	for(i=0;i<size;i++)
	{
	if
	{
	arr[i]==item;
	return 1;
	}
	}
}

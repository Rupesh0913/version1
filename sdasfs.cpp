#include <stdio.h>
void calculate_the_maximum(int , int ); 

void calculate_the_maximum(int n, int k) {
//int sum;
n+k;
//return n+k;
}

int main() {
    int n, k;
  int sum=0;
    scanf("%d %d", &n, &k);
    sum=calculate_the_maximum(n, k);
 printf("%d",sum);
    return 0;
}
